# Hugo configuration file
title: BetterGPT Docs

# import hextra as module
module:
  imports:
    - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: Status
      url: "https://stats.uptimerobot.com/MgzZgSuQLY"
      weight: 1
    - name: BetterGPT.lol
      url: "https://bettergpt.lol"
      weight: 2
    - name: Search
      weight: 3
      params:
        type: search

params:
  navbar:
    displayTitle: true
    displayLogo: false

  footer:
    displayCopyright: false
    displayPoweredBy: true

  editURL:
    enable: true
    base: "https://github.com/michaewlt/betterdocs/edit/main/content"
